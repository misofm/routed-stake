[package]
name = "routed_stake"
edition = "2024"
license = "Apache 2.0"
authors = ["Miso Labs, Inc. (build@miso.network)"]

[dependencies]
# TODO(release): re-pin to the pushed `exact-accounting` commit of misofm/royalty-pool.
royalty_pool = { local = "../royalty-pool" }
